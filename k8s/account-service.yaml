apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service
  namespace: fintech-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-service
  template:
    metadata:
      labels:
        app: account-service
    spec:
      containers:
        - name: account-service
          image: account-service:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: fintech-demo
spec:
  selector:
    app: account-service
  ports:
    - port: 8000
      targetPort: 8000
