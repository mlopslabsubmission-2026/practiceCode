apiVersion: apps/v1
kind: Deployment
metadata:
  name: fee-service
  namespace: fintech-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fee-service
  template:
    metadata:
      labels:
        app: fee-service
    spec:
      containers:
        - name: fee-service
          image: fee-service:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: fee-service
  namespace: fintech-demo
spec:
  selector:
    app: fee-service
  ports:
    - port: 8000
      targetPort: 8000
